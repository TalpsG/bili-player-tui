# KEYBINDINGS.md

按键绑定参考文档。**以本文件为准**，`help_view.rs` 中的帮助弹窗与本文件保持同步。

---

## 一、Normal 模式（主界面）

### 全局（与列焦点无关）

| 按键 | 功能 |
|------|------|
| `q` / `Ctrl+C` | 退出程序 |
| `Ctrl+L` | 强制重绘（修复 tmux 切换后图片消失问题） |
| `/` | 打开搜索弹窗 |
| `?` | 打开帮助弹窗 |
| `Tab` / `Shift+Tab` | 在可见列之间循环切换焦点 |
| `Space` | 暂停 / 继续播放 |
| `n` | 下一首 |
| `p` | 上一首 |
| `←` / `→` | 快退 / 快进 5 秒（始终有效，不受列焦点影响） |
| `m` | 静音切换（同时显示音量弹窗） |
| `r` | 循环切换播放模式（顺序 → 列表循环 → 单曲循环 → 随机） |

### 列焦点导航（h / l）

| 按键 | 当前焦点列 | 效果 |
|------|-----------|------|
| `h` | 歌单列 | 无操作（已是最左列） |
| `h` | 曲目列 | 焦点移到歌单列 |
| `h` | 详情列 | 焦点移到曲目列 |
| `l` | 歌单列 | 焦点移到曲目列 |
| `l` | 曲目列 | 焦点移到详情列 |
| `l` | 详情列 | 无操作（已是最右列） |
| `Enter` | 歌单列 | 焦点移到曲目列（查看歌单内容） |

### 光标移动（列表列 vs 详情列）

| 按键 | 歌单列 / 曲目列 | 详情列 |
|------|----------------|--------|
| `j` / `↓` | 光标下移一行 | 音量 -5 |
| `k` / `↑` | 光标上移一行 | 音量 +5 |
| `J` / `PageDown` | 光标下移 10 行 | — |
| `K` / `PageUp` | 光标上移 10 行 | — |
| `g` / `Home` | 跳到列表顶部 | — |
| `G` / `End` | 跳到列表底部 | — |

### 曲目 / 歌单操作

| 按键 | 焦点列 | 功能 |
|------|--------|------|
| `Enter` | 曲目列 | 播放选中曲目（队列视图）；或用歌单替换队列并播放（歌单视图） |
| `a` | 曲目列（歌单视图） | 把当前选中曲目加入队列（有 bvid 去重，已在队列则跳转播放） |
| `A`（Shift+A） | 任意列 | 打开"添加到歌单"选择弹窗 |
| `c` | 歌单列 | 打开新建歌单输入弹窗 |
| `d` | 歌单列 | 打开删除歌单确认弹窗（光标在队列时无效） |
| `d` | 曲目列（队列视图） | 从队列删除当前选中曲目 |
| `d` | 曲目列（歌单视图） | 从歌单删除当前选中曲目 |

---

## 二、搜索弹窗

### SearchInput 模式（输入状态，光标在搜索框）

| 按键 | 功能 |
|------|------|
| 任意字符 | 在搜索框光标处插入字符 |
| `Backspace` | 删除光标前一个字符 |
| `Enter` | 提交搜索（关键词搜索），或直接播放（若输入为 BV 号 / B 站链接） |
| `Esc` | 关闭搜索弹窗，回到 Normal 模式 |

> **BV 号直接播放**：在搜索框中输入 BV 号（如 `BV1xx411c7mD`）或完整 B 站视频链接，按 `Enter` 后直接拉取视频信息并播放，不走搜索 API。

### SearchNormal 模式（结果导航状态）

| 按键 | 功能 |
|------|------|
| `j` / `↓` | 结果列表光标下移 |
| `k` / `↑` | 结果列表光标上移 |
| `i` | 切回 SearchInput 模式（编辑查询词，光标保持原位） |
| `a` | 切回 SearchInput 模式（追加模式，光标移到末尾） |
| `Enter` | 播放选中搜索结果（自动加入队列） |
| `A`（Shift+A） | 把选中搜索结果加入指定歌单（打开选择弹窗） |
| `q` / `Esc` | 关闭搜索弹窗 |

---

## 三、Popup 按键

### 新建歌单（PlaylistCreate）

| 按键 | 功能 |
|------|------|
| 任意字符 | 输入歌单名称 |
| `Backspace` | 删除最后一个字符 |
| `Enter` | 确认创建（名称不能为空） |
| `Esc` | 取消，关闭弹窗 |

### 删除歌单确认（PlaylistDeleteConfirm）

| 按键 | 功能 |
|------|------|
| `Enter` / `y` | 确认删除 |
| `Esc` / `n` / `q` | 取消删除 |

### 添加到歌单（AddToPlaylist）

| 按键 | 功能 |
|------|------|
| `j` / `↓` | 选择下一个歌单 |
| `k` / `↑` | 选择上一个歌单 |
| `Enter` | 确认添加到选中歌单 |
| `Esc` / `q` | 取消，关闭弹窗 |

### 帮助（Help）

| 按键 | 功能 |
|------|------|
| `?` / `Esc` / `q` | 关闭帮助弹窗 |

---

## 四、Popup 优先级（z-order，从高到低）

当多个 popup 同时存在于栈上时，按以下优先级拦截键盘输入：

| 优先级 | PopupLayer | 触发方式 |
|--------|-----------|---------|
| 1（最高） | Help | `?` |
| 2 | AddToPlaylist | `A`（Shift+A） |
| 3 | PlaylistDeleteConfirm | `d`（歌单列焦点） |
| 4 | PlaylistCreate | `c`（歌单列焦点） |
| 5 | Search | `/` |
| 6（最低） | VolumeSlider | `↑`/`↓` 调音量时自动出现，3 秒后消失 |

---

## 五、按键设计约定

- **`q` 始终可退出或关闭当前弹窗**（全局）
- **`Esc` 始终取消**（关闭弹窗或退出当前模式）
- **`←` / `→` 始终 seek**，不被列焦点改变（区别于 `h`/`l`）
- **`h` / `l` 仅在列间移动焦点**，不做 seek
- **`j` / `k` 和 `↑` / `↓` 在歌单列 / 曲目列**移动光标；**在详情列**控制音量
- **大写字母（`A`、`J`、`K`、`G`）** 用于"扩展"语义（Shift+A = 添加到歌单；J/K = 翻页；G = 跳底）
- **`s`** 已改为 Noop，不再使用
