# bili-player-cli 功能规格说明 (Specification)

## 1. 概述

bili-player-cli 是一个终端音乐播放器，播放来自 Bilibili 视频的音频流。支持搜索、歌单管理、封面显示。

---

## 2. 功能需求

### 2.1 搜索

| ID | 功能 | 优先级 | 说明 |
|----|------|--------|------|
| S-1 | 关键词搜索视频 | P0 | 输入关键词，返回视频搜索结果列表 |
| S-2 | 搜索结果展示 | P0 | 显示标题、UP主、时长、封面缩略图 |
| S-3 | 搜索结果翻页 | P1 | 支持翻页加载更多结果 |
| S-4 | 搜索历史 | P2 | 记录最近搜索关键词 |
| S-5 | 按 BV 号/URL 直接播放 | P0 | 输入 BV 号或视频链接直接加入队列播放 |

**验收标准:**
- [ ] 输入关键词后 3 秒内显示搜索结果（网络正常）
- [ ] 搜索结果至少包含标题、UP主、时长
- [ ] 结果可滚动浏览
- [ ] BV 号和 URL 均可识别并播放

### 2.2 播放

| ID | 功能 | 优先级 | 说明 |
|----|------|--------|------|
| P-1 | 播放音频流 | P0 | 流式播放，不需完整下载 |
| P-2 | 暂停/继续 | P0 | 空格键切换 |
| P-3 | 下一首/上一首 | P0 | n/p 键 |
| P-4 | 快进/快退 | P0 | 左右方向键，默认 ±5 秒 |
| P-5 | 跳转到指定位置 | P1 | 点击进度条或输入时间 |
| P-6 | 音量调节 | P0 | 上下方向键或 +/- 键 |
| P-7 | 静音切换 | P1 | m 键 |
| P-9 | 音频质量选择 | P1 | 优先 FLAC > 杜比 > 高码率 > 标准 |
| P-10 | 播放进度显示 | P0 | 进度条 + 当前时间/总时长 |

**验收标准:**
- [ ] 点击播放后 2 秒内开始出声（网络正常）
- [ ] 暂停/继续无杂音
- [ ] seek 后 1 秒内恢复播放
- [ ] 音量调节平滑无爆音
- [ ] 进度条实时更新（250ms 精度）

### 2.3 队列管理

| ID | 功能 | 优先级 | 说明 |
|----|------|--------|------|
| Q-1 | 播放队列 | P0 | 当前播放队列，顺序播放 |
| Q-2 | 添加到队列 | P0 | 从搜索结果添加到队列末尾 |
| Q-3 | 插入到下一首 | P1 | 添加到当前曲目之后 |
| Q-4 | 从队列移除 | P1 | 删除指定曲目 |
| Q-5 | 清空队列 | P1 | 一键清空 |
| Q-6 | Shuffle | P1 | 随机播放，保持原始顺序不变（排列索引方案） |
| Q-7 | Repeat | P1 | 三种模式：关/列表循环/单曲循环 |
| Q-8 | 队列持久化 | P1 | 退出时保存，启动时恢复 |

**验收标准:**
- [ ] Shuffle 切换时当前曲目不中断
- [ ] Shuffle 后再次切换回顺序播放，恢复原始顺序
- [ ] Repeat Track 模式下单曲结束后自动重播
- [ ] 队列持久化后重启可恢复（曲目信息完整）

### 2.4 歌单

| ID | 功能 | 优先级 | 说明 |
|----|------|--------|------|
| PL-1 | 创建歌单 | P0 | 输入歌单名称创建 |
| PL-2 | 歌单列表 | P0 | 左侧面板显示所有歌单 |
| PL-3 | 添加曲目到歌单 | P0 | 从搜索结果或队列添加 |
| PL-4 | 从歌单移除曲目 | P1 | |
| PL-5 | 删除歌单 | P1 | 确认后删除 |
| PL-6 | 播放歌单 | P0 | 将歌单曲目加入队列 |
| PL-7 | 歌单持久化 | P0 | JSON 文件存储 |
| PL-8 | 导入 B 站收藏夹 | P1 | 输入收藏夹 ID 导入 |
| PL-9 | 导入 UP 主视频列表 | P2 | |
| PL-10 | 重命名歌单 | P2 | |

**验收标准:**
- [ ] 歌单在退出重启后保留
- [ ] 导入收藏夹后曲目信息完整（标题、UP主、时长）
- [ ] 歌单曲目上限至少 1000 首

### 2.5 认证

| ID | 功能 | 优先级 | 说明 |
|----|------|--------|------|
| A-1 | SESSDATA 配置 | P0 | 在 config.toml 中配置 |
| A-2 | 未登录提示 | P0 | 未配置时提示，限制音质 |
| A-3 | 登录状态显示 | P1 | 界面显示当前用户名或"未登录" |
| A-4 | SESSDATA 验证 | P1 | 启动时验证有效性，无效时提示 |

**验收标准:**
- [ ] 无 SESSDATA 时仍可播放（低音质）
- [ ] 无效 SESSDATA 时显示清晰错误信息
- [ ] SESSDATA 不出现在日志中

### 2.6 TUI 界面

| ID | 功能 | 优先级 | 说明 |
|----|------|--------|------|
| T-1 | 主布局 | P0 | 左侧歌单/队列 + 右侧封面信息 + 底部控制 |
| T-2 | 封面图显示 | P1 | 使用 ratatui-image，支持 Sixel/Kitty/iTerm2/Halfblocks |
| T-3 | 搜索输入 | P0 | 按 / 进入搜索模式，Esc 退出 |
| T-4 | 进度条 | P0 | 可视化进度，显示时间 |
| T-5 | 快捷键提示 | P0 | 底部显示常用按键 |
| T-6 | 帮助页面 | P1 | ? 键显示完整快捷键列表 |
| T-7 | 颜色主题 | P2 | 支持切换 dark/light 等主题 |
| T-8 | 窗口大小自适应 | P0 | 终端缩放时布局自动调整 |
| T-9 | 中文对齐 | P1 | CJK 字符正确计算宽度 |

**验收标准:**
- [ ] 在 80x24 最小终端尺寸下可用
- [ ] 封面图在不支持的终端自动降级为 halfblocks
- [ ] 搜索模式下正常输入中文
- [ ] 终端缩放不崩溃，布局不乱

### 2.7 CLI 接口

| ID | 功能 | 优先级 | 说明 |
|----|------|--------|------|
| C-1 | 启动 TUI | P0 | `bili-player-cli` 无参数启动 |
| C-2 | 直接播放 | P0 | `bili-player-cli play BV1xx...` 或 URL |
| C-3 | 指定配置 | P2 | `--config path/to/config.toml` |
| C-4 | 调试模式 | P2 | `--debug` 输出详细日志 |

---

## 3. 非功能需求

### 3.1 性能

| 指标 | 目标 |
|------|------|
| 启动时间 | < 1 秒（冷启动，不含网络） |
| 搜索响应 | < 3 秒（网络正常） |
| 播放启动延迟 | < 2 秒（从点击到出声） |
| 内存占用 | < 100 MB（典型使用） |
| CPU 占用 | < 5%（播放时，不含解码） |
| TUI 刷新 | 4 FPS（250ms tick） |

### 3.2 跨平台

| 平台 | 支持等级 |
|------|----------|
| macOS (x86_64, aarch64) | P0 |
| Linux (x86_64) | P0 |
| Windows (x86_64) | P1 |

### 3.3 可靠性

- TUI 不因任何运行时错误崩溃（网络、API、播放器错误均显示在状态栏）
- 网络断开后重连自动恢复播放
- 异常退出不丢失歌单数据（每次变更后立即持久化）

### 3.4 安全

- SESSDATA 不写入日志
- 配置文件权限检查（仅当前用户可读）
- 不执行外部命令（除 mpv 后端加载 libmpv 外无外部依赖）

---

## 4. 数据模型

### Track

```
Track {
    bvid: String,          // BV 号，唯一标识
    cid: u64,              // 分 P 的 cid
    title: String,         // 标题
    author: String,        // UP主
    duration: Duration,    // 时长
    cover_url: Option<String>,  // 封面 URL
    source: Option<TrackSource>, // 音频流（懒解析）
}
```

### TrackSource

```
TrackSource {
    stream_url: String,    // 音频流 URL
    audio_quality: AudioQuality,
    referer: String,       // 反盗链 Referer
}
```

### AudioQuality

```
AudioQuality {
    Flac | DolbyAtmos | Dash { bitrate: u32 } | LegacyMp4
}
```

### Playlist

```
Playlist {
    id: u64,
    name: String,
    tracks: Vec<Track>,
    source: Option<PlaylistSource>,  // 导入来源（收藏夹 ID 等）
}
```

---

## 5. B 站 API 规格

### 5.1 WBI 签名

所有 `/wbi/` 路径的 API 需要 WBI 签名。

**流程:**
1. GET `/x/web-interface/nav` → 提取 `wbi_img.img_url` 和 `wbi_img.sub_url` 的文件名作为 `img_key` 和 `sub_key`
2. 拼接 `img_key + sub_key`，按 64 元素置换表重排，取前 32 字符为 `mixin_key`
3. 请求参数按 key 字典序排列，过滤 `!'()*` 字符，URL 编码
4. 追加 `wts=<当前时间戳>`
5. 计算 `MD5(排序后的查询字符串 + mixin_key)` → `w_rid`
6. 将 `w_rid` 和 `wts` 加入请求参数

**WBI key 缓存:** 有效期约数小时，过期后自动刷新。

### 5.2 核心端点

| 端点 | 用途 | 鉴权 |
|------|------|------|
| `/x/web-interface/search/type` | 搜索 | WBI |
| `/x/web-interface/wbi/view` | 视频信息 | WBI |
| `/x/player/wbi/playurl` | 音频流 URL | WBI + SESSDATA |
| `/x/v3/fav/folder/created/list-all` | 收藏夹列表 | SESSDATA |
| `/x/v3/fav/resource/list` | 收藏夹内容 | SESSDATA |
| `/x/web-interface/nav` | 用户信息 + WBI key | SESSDATA |

### 5.3 音频流解析优先级

请求 `fnval=16` (DASH 格式) 后，按以下优先级选择音频:

1. **FLAC** (quality=30251) — 无损
2. **Dolby Atmos** (quality=30250) — 杜比全景声
3. **DASH audio** (quality=30280/30232/30216) — 选最高码率
4. **Legacy MP4** (durl) — 旧格式 fallback

所有 CDN 请求需带 `Referer: https://www.bilibili.com/`。

---

## 6. 键位绑定

### 全局

| 按键 | 功能 |
|------|------|
| `q` / `Ctrl+C` | 退出 |
| `/` | 进入搜索 |
| `?` | 帮助 |
| `1-9` | 切换到对应歌单 |
| `Tab` | 切换面板焦点 |

### 播放控制

| 按键 | 功能 |
|------|------|
| `Space` | 暂停/继续 |
| `n` | 下一首 |
| `p` | 上一首 |
| `h` / `←` | 快退 5 秒 |
| `l` / `→` | 快进 5 秒 |
| `k` / `↑` | 音量 +5 |
| `j` / `↓` | 音量 -5 |
| `m` | 静音切换 |
| `s` | Shuffle 切换 |
| `r` | Repeat 模式切换 (关→列表→单曲) |

### 列表导航

| 按键 | 功能 |
|------|------|
| `j` / `↓` | 下移光标 |
| `k` / `↑` | 上移光标 |
| `J` / `PageDown` | 下翻一页 |
| `K` / `PageUp` | 上翻一页 |
| `g` / `Home` | 跳到顶部 |
| `G` / `End` | 跳到底部 |
| `Enter` | 播放选中曲目 |
| `a` | 添加到歌单 |
| `d` | 从队列移除 |

> **注意**: j/k 在播放控制和列表导航中功能不同，取决于当前焦点面板。播放控制中 j/k 调音量，列表导航中 j/k 移动光标。

### 搜索模式

| 按键 | 功能 |
|------|------|
| `Enter` | 执行搜索 |
| `Esc` | 退出搜索 |
| `Enter` (在结果上) | 播放选中结果 |

---

## 7. 测试策略

### 7.1 单元测试

| 模块 | 测试内容 | 优先级 |
|------|----------|--------|
| `bilibili/wbi` | WBI 签名算法正确性 | P0 |
| `bilibili/wbi` | mixin key 推导 | P0 |
| `bilibili/search` | 搜索结果解析 | P0 |
| `bilibili/video` | 视频信息解析 | P0 |
| `bilibili/stream` | DASH 响应解析 + 音频质量选择 | P0 |
| `bilibili/favorite` | 收藏夹数据解析 | P1 |
| `queue` | 队列操作 (添加/移除/跳转) | P0 |
| `queue` | Shuffle 排列生成与切换 | P0 |
| `queue` | Repeat 模式切换 | P0 |
| `playlist` | 歌单 CRUD | P0 |
| `playlist/storage` | JSON 序列化/反序列化 | P0 |
| `config` | 配置加载/保存 | P1 |
| `command` | 命令解析 | P1 |

### 7.2 集成测试

| 场景 | 测试内容 | 优先级 |
|------|----------|--------|
| 搜索到播放 | 搜索关键词 → 选择结果 → 获取流 URL → 播放 | P0 |
| B 站 API 实际调用 | 用真实 SESSDATA 调用各端点，验证响应格式 | P1 |
| 歌单持久化 | 创建歌单 → 添加曲目 → 重启 → 验证恢复 | P0 |
| 队列恢复 | 添加队列 → 退出 → 重启 → 验证恢复 | P1 |
| 收藏夹导入 | 导入收藏夹 → 验证曲目完整 | P1 |

### 7.3 API Mock 测试

使用 `mockito` mock B 站 API 响应，测试:
- WBI 签名是否正确附加到请求
- 各种 API 响应格式解析
- 错误响应处理 (403, 412, -352 等)
- WBI key 过期后自动刷新

### 7.4 TUI 测试

- 可测试部分: 数据模型到 UI 状态的映射、命令处理逻辑
- 不做像素级渲染测试（维护成本过高）
- 手动测试清单: 见下方

### 7.5 手动测试清单

- [ ] 启动 TUI，确认布局正常
- [ ] 搜索关键词，确认结果列表
- [ ] 播放曲目，确认音频输出
- [ ] 暂停/继续，确认状态切换
- [ ] 下一首/上一首，确认切换正常
- [ ] 音量调节，确认变化
- [ ] Shuffle 切换，确认随机播放
- [ ] Repeat 切换，确认三种模式
- [ ] 创建歌单，添加曲目，重启后确认保留
- [ ] 封面图显示（在支持图片的终端中）
- [ ] 窗口缩放，确认布局自适应
- [ ] 无 SESSDATA 启动，确认降级提示
- [ ] 无效 SESSDATA，确认错误提示
- [ ] 网络断开后恢复，确认行为
- [ ] `bili-player-cli play BV1xx...` 直接播放

---

## 8. 交付分期

### Phase 1: 核心播放

目标: 能搜、能播、能控制。

- CLI 参数解析
- B 站 API 客户端 (WBI 签名 + 搜索 + 视频信息 + 音频流)
- mpv 播放后端
- 基础 TUI (队列列表 + 正在播放 + 进度条)
- 播放控制 (暂停/继续/上下曲/seek/音量)
- SESSDATA 配置
- 单元测试: WBI 签名、队列、API 解析

### Phase 2: 歌单与封面

目标: 可用性提升。

- 歌单 CRUD + 持久化
- 队列持久化
- 封面图显示
- Shuffle + Repeat
- 导入 B 站收藏夹
- BV 号/URL 直接播放
- 集成测试

### Phase 3: 完善

目标: 体验打磨。

- 音量归一化
- 快捷键自定义
- 搜索翻页/历史
- MPRIS (Linux)
- 主题切换
