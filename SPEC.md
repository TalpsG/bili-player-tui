# bili-player-cli 功能规格说明 (Specification)

## 1. 概述

bili-player-cli 是一个终端音乐播放器，播放来自 Bilibili 视频的音频流。支持搜索、歌单管理、封面显示。

---

## 2. 功能需求

### 2.1 搜索

| ID | 功能 | 优先级 | 说明 |
|----|------|--------|------|
| S-1 | 关键词搜索视频 | P0 | 输入关键词，返回视频搜索结果列表 |
| S-2 | 搜索结果展示 | P0 | 显示标题、UP主、时长、封面缩略图 |
| S-3 | 搜索结果翻页 | P1 | 支持翻页加载更多结果 |
| S-4 | 搜索历史 | P2 | 记录最近搜索关键词 |
| S-5 | 按 BV 号/URL 直接播放 | P0 | 输入 BV 号或视频链接直接加入队列播放 |

**验收标准:**
- [ ] 输入关键词后 3 秒内显示搜索结果（网络正常）
- [ ] 搜索结果至少包含标题、UP主、时长
- [ ] 结果可滚动浏览
- [ ] BV 号和 URL 均可识别并播放

### 2.2 播放

| ID | 功能 | 优先级 | 说明 |
|----|------|--------|------|
| P-1 | 播放音频流 | P0 | 流式播放，不需完整下载 |
| P-2 | 暂停/继续 | P0 | 空格键切换 |
| P-3 | 下一首/上一首 | P0 | n/p 键 |
| P-4 | 快进/快退 | P0 | 左右方向键，默认 ±5 秒 |
| P-5 | 跳转到指定位置 | P1 | 点击进度条或输入时间 |
| P-6 | 音量调节 | P0 | 上下方向键或 +/- 键 |
| P-7 | 静音切换 | P1 | m 键 |
| P-9 | 音频质量选择 | P1 | 优先 FLAC > 杜比 > 高码率 > 标准 |
| P-10 | 播放进度显示 | P0 | 进度条 + 当前时间/总时长 |

**验收标准:**
- [ ] 点击播放后 2 秒内开始出声（网络正常）
- [ ] 暂停/继续无杂音
- [ ] seek 后 1 秒内恢复播放
- [ ] 音量调节平滑无爆音
- [ ] 进度条实时更新（250ms 精度）

### 2.3 队列管理

| ID | 功能 | 优先级 | 说明 |
|----|------|--------|------|
| Q-1 | 播放队列 | P0 | 当前播放队列，顺序播放 |
| Q-2 | 添加到队列 | P0 | 从搜索结果添加到队列末尾 |
| Q-3 | 插入到下一首 | P1 | 添加到当前曲目之后 |
| Q-4 | 从队列移除 | P1 | 删除指定曲目 |
| Q-5 | 清空队列 | P1 | 一键清空 |
| Q-6 | Shuffle | P1 | 随机播放，保持原始顺序不变（排列索引方案） |
| Q-7 | Repeat | P1 | 三种模式：关/列表循环/单曲循环 |
| Q-8 | 队列持久化 | P1 | 退出时保存，启动时恢复 |

**验收标准:**
- [ ] Shuffle 切换时当前曲目不中断
- [ ] Shuffle 后再次切换回顺序播放，恢复原始顺序
- [ ] Repeat Track 模式下单曲结束后自动重播
- [ ] 队列持久化后重启可恢复（曲目信息完整）

### 2.4 歌单

| ID | 功能 | 优先级 | 说明 |
|----|------|--------|------|
| PL-1 | 创建歌单 | P0 | 焦点在歌单栏按 `c`，弹出居中输入 popup，输入名称后 Enter 确认 |
| PL-2 | 歌单列表 | P0 | 左侧面板显示"Queue (N)"及所有用户歌单 |
| PL-3 | 添加曲目到歌单 | P0 | `Shift+A` 弹出歌单选择 popup，可从搜索结果或曲目列表操作 |
| PL-4 | 从歌单移除曲目 | P1 | 焦点在中栏且当前为歌单视图时，`d` 从歌单移除高亮曲目 |
| PL-5 | 删除歌单 | P1 | 焦点在歌单栏按 `d`，弹出确认 popup，Enter/y 确认，Esc/n 取消 |
| PL-6 | 播放歌单 | P0 | 在歌单视图中 Enter 选中曲目，将整个歌单替换队列并从该曲目开始播放 |
| PL-7 | 歌单持久化 | P0 | JSON 文件存储（`playlists.json`），每次变更后立即写盘 |
| PL-9 | 导入 UP 主视频列表 | P2 | |
| PL-10 | 重命名歌单 | P2 | |

**验收标准:**
- [ ] 歌单在退出重启后保留
- [ ] 歌单曲目上限至少 1000 首

### 2.5 认证

| ID | 功能 | 优先级 | 说明 |
|----|------|--------|------|
| A-1 | SESSDATA 配置 | P0 | 在 config.toml 中配置 |
| A-2 | 未登录提示 | P0 | 未配置时提示，限制音质 |
| A-3 | 登录状态显示 | P1 | 界面显示当前用户名或"未登录" |
| A-4 | SESSDATA 验证 | P1 | 启动时验证有效性，无效时提示 |

**验收标准:**
- [ ] 无 SESSDATA 时仍可播放（低音质）
- [ ] 无效 SESSDATA 时显示清晰错误信息
- [ ] SESSDATA 不出现在日志中

### 2.6 TUI 界面

| ID | 功能 | 优先级 | 说明 |
|----|------|--------|------|
| T-1 | 三栏主布局 | P0 | 歌单列表 \| 曲目列表 \| 曲目详情，yazi/ranger 风格 |
| T-2 | Header | P0 | 1 行，左 app 名称，右登录状态 |
| T-3 | Status | P0 | 1 行，Gauge 进度条 + 时间 + 音量图标+数值 |
| T-4 | Popup Overlay 系统 | P0 | z-order 层叠渲染，anchor 定位，painter's algorithm |
| T-5 | 搜索 overlay | P0 | 居中 popup，输入框+结果列表，`/` 触发 |
| T-6 | 帮助 overlay | P0 | 全屏快捷键列表，`?` 触发 |
| T-7 | 音量滑钮 overlay | P0 | 状态栏上方居中，`↑`/`↓` 调音量时弹出，自动消失 |
| T-8 | 窄终端自适应 | P0 | ≥80 列三栏，50-79 列两栏，<50 列仅中栏 |
| T-9 | 封面图显示 ✅ | P1 | 右栏曲目详情区，StatefulImage widget，Sixel/Kitty/iTerm2/Halfblocks，占上半部分高度，含磁盘 LRU 缓存 |
| T-10 | 颜色主题 | P2 | 支持切换 dark/light 等主题 |
| T-11 | 中文对齐 | P1 | CJK 字符正确计算宽度 |
| T-12 | 创建歌单 popup | P0 | 居中输入框，`c` 触发，Enter 确认，Esc 取消 |
| T-13 | 删除歌单确认 popup | P0 | 居中确认框，红色边框，显示歌单名，Enter/y 删除，Esc/n 取消 |
| T-14 | 添加到歌单选择 popup | P0 | 居中列表选择框，`Shift+A` 触发，j/k 移动，Enter 确认，Esc 取消 |

**验收标准:**
- [ ] 在 80x24 最小终端尺寸下可用
- [ ] 封面图在不支持的终端自动降级为 halfblocks
- [ ] 搜索模式下正常输入中文
- [ ] 终端缩放不崩溃，布局按窄终端规则自适应
- [ ] Popup 按正确 z-order 层叠渲染

### 2.7 CLI 接口

| ID | 功能 | 优先级 | 说明 |
|----|------|--------|------|
| C-1 | 启动 TUI | P0 | `bili-player-cli` 无参数启动 |
| C-2 | 直接播放 | P0 | `bili-player-cli play BV1xx...` 或 URL |
| C-3 | 指定配置 | P2 | `--config path/to/config.toml` |
| C-4 | 调试模式 | P2 | `--debug` 输出详细日志 |

---

## 3. 非功能需求

### 3.1 性能

| 指标 | 目标 |
|------|------|
| 启动时间 | < 1 秒（冷启动，不含网络） |
| 搜索响应 | < 3 秒（网络正常） |
| 播放启动延迟 | < 2 秒（从点击到出声） |
| 内存占用 | < 100 MB（典型使用） |
| CPU 占用 | < 5%（播放时，不含解码） |
| TUI 刷新 | 4 FPS（250ms tick） |

### 3.2 跨平台

| 平台 | 支持等级 |
|------|----------|
| macOS (x86_64, aarch64) | P0 |
| Linux (x86_64) | P0 |
| Windows (x86_64) | P1 |

### 3.3 可靠性

- TUI 不因任何运行时错误崩溃（网络、API、播放器错误均显示在状态栏）
- 网络断开后重连自动恢复播放
- 异常退出不丢失歌单数据（每次变更后立即持久化）

### 3.4 安全

- SESSDATA 不写入日志
- 配置文件权限检查（仅当前用户可读）
- 不执行外部命令（除 mpv 后端加载 libmpv 外无外部依赖）

---

## 4. 数据模型

### Track

```
Track {
    bvid: String,          // BV 号，唯一标识
    cid: u64,              // 分 P 的 cid
    title: String,         // 标题
    author: String,        // UP主
    duration: Duration,    // 时长
    cover_url: Option<String>,  // 封面 URL
    source: Option<TrackSource>, // 音频流（懒解析）
}
```

### TrackSource

```
TrackSource {
    stream_url: String,    // 音频流 URL
    audio_quality: AudioQuality,
    referer: String,       // 反盗链 Referer
}
```

### AudioQuality

```
AudioQuality {
    Flac | DolbyAtmos | Dash { bitrate: u32 } | LegacyMp4
}
```

### Playlist

```
Playlist {
    id: u64,
    name: String,
    tracks: Vec<Track>,
}
```

---

## 5. B 站 API 规格

### 5.1 WBI 签名

所有 `/wbi/` 路径的 API 需要 WBI 签名。

**流程:**
1. GET `/x/web-interface/nav` → 提取 `wbi_img.img_url` 和 `wbi_img.sub_url` 的文件名作为 `img_key` 和 `sub_key`
2. 拼接 `img_key + sub_key`，按 64 元素置换表重排，取前 32 字符为 `mixin_key`
3. 请求参数按 key 字典序排列，过滤 `!'()*` 字符，URL 编码
4. 追加 `wts=<当前时间戳>`
5. 计算 `MD5(排序后的查询字符串 + mixin_key)` → `w_rid`
6. 将 `w_rid` 和 `wts` 加入请求参数

**WBI key 缓存:** 有效期约数小时，过期后自动刷新。

### 5.2 核心端点

| 端点 | 用途 | 鉴权 |
|------|------|------|
| `/x/web-interface/search/type` | 搜索 | WBI |
| `/x/web-interface/wbi/view` | 视频信息 | WBI |
| `/x/player/wbi/playurl` | 音频流 URL | WBI + SESSDATA |
| `/x/web-interface/nav` | 用户信息 + WBI key | SESSDATA |

### 5.3 音频流解析优先级

请求 `fnval=16` (DASH 格式) 后，按以下优先级选择音频:

1. **FLAC** (quality=30251) — 无损
2. **Dolby Atmos** (quality=30250) — 杜比全景声
3. **DASH audio** (quality=30280/30232/30216) — 选最高码率
4. **Legacy MP4** (durl) — 旧格式 fallback

所有 CDN 请求需带 `Referer: https://www.bilibili.com/`。

---

## 6. 键位绑定

### 输入模式

| 模式 | 触发 | 退出 |
|------|------|------|
| Normal | 默认 | — |
| Search Input | `/` | `Esc` / `Enter` |
| Search Normal | `Enter`/`Esc` 结束输入后 | `Esc` / `q` |

> **注意**: 歌单创建、歌单删除确认、添加到歌单选择均通过 Popup overlay 实现，不引入新的输入模式。

### 全局 (Normal 模式)

| 按键 | 功能 |
|------|------|
| `q` / `Ctrl+C` | 退出 |
| `/` | 打开搜索 overlay |
| `?` | 打开帮助 overlay |
| `Tab` / `Shift+Tab` | 切换三栏焦点（仅在可见栏之间循环） |

### 列焦点导航 (h/l，ranger/yazi 风格)

| 按键 | 焦点位置 | 功能 |
|------|----------|------|
| `h` | 歌单栏 | 无操作（已在最左侧） |
| `h` | 曲目栏 | 焦点移到歌单栏 |
| `h` | 详情栏 | 焦点移到曲目栏 |
| `l` | 歌单栏 | 焦点移到曲目栏 |
| `l` | 曲目栏 | 焦点移到详情栏 |
| `l` | 详情栏 | 无操作（已在最右侧） |
| `Enter` | 歌单栏 | 焦点移到曲目栏 |
| `Enter` | 曲目栏/详情栏 | 播放选中曲目 |

### 播放控制

| 按键 | 功能 |
|------|------|
| `Space` | 暂停/继续 |
| `n` | 下一首 |
| `p` | 上一首 |
| `←` / `→` | 快退/快进 5 秒（任意焦点下均生效） |
| `j` / `↓` | 焦点在详情栏时：音量 -5 |
| `k` / `↑` | 焦点在详情栏时：音量 +5 |
| `↑` / `↓` | 焦点在列表栏时：光标移动；焦点在详情栏时：音量 ±5 |
| `m` | 静音切换 |
| `s` | Shuffle 切换 |
| `r` | Repeat 模式切换 (关→列表→单曲) |

### 列表导航 (焦点在歌单栏或曲目栏时)

| 按键 | 功能 |
|------|------|
| `j` / `↓` | 下移光标 |
| `k` / `↑` | 上移光标 |
| `J` / `PageDown` | 下翻一页 |
| `K` / `PageUp` | 上翻一页 |
| `g` / `Home` | 跳到顶部 |
| `G` / `End` | 跳到底部 |
| `Enter` (队列视图) | 播放选中曲目 |
| `Enter` (歌单视图) | 将整个歌单替换队列，从选中位置开始播放 |
| `a` | 添加选中曲目到队列（去重；若已存在则跳转并立即播放） |
| `A` (`Shift+A`) | 弹出"添加到歌单"选择 popup |
| `d` (队列视图) | 从队列移除选中曲目 |
| `d` (歌单视图) | 从当前歌单移除选中曲目 |
| `c` (焦点在歌单栏) | 弹出"创建歌单"输入 popup |
| `d` (焦点在歌单栏) | 弹出"删除歌单"确认 popup（Queue 行不可删除） |

### 搜索模式 (Search Input)

| 按键 | 功能 |
|------|------|
| 字符键 | 输入搜索关键词 |
| `Backspace` | 删除字符 |
| `Enter` | 执行搜索，切换到 Search Normal |
| `Esc` | 关闭搜索 overlay |

### 搜索结果浏览 (Search Normal)

| 按键 | 功能 |
|------|------|
| `j` / `↓` | 下移光标 |
| `k` / `↑` | 上移光标 |
| `Enter` | 将选中结果加入队列并播放 |
| `a` | 将选中结果加入队列（不自动播放，去重）/ 回到 Search Input（光标置尾） |
| `A` (`Shift+A`) | 弹出"添加到歌单"选择 popup |
| `i` | 回到 Search Input 模式（光标置首） |
| `Esc` / `q` | 关闭搜索 overlay |

### Popup 键位

| Popup | 按键 | 功能 |
|-------|------|------|
| 创建歌单 | 字符键 | 输入歌单名称 |
| 创建歌单 | `Backspace` | 删除字符 |
| 创建歌单 | `Enter` | 确认创建（名称为空时显示错误） |
| 创建歌单 | `Esc` | 取消 |
| 删除歌单确认 | `Enter` / `y` | 确认删除 |
| 删除歌单确认 | `Esc` / `n` / `q` | 取消 |
| 添加到歌单 | `j` / `↓` | 下移选择 |
| 添加到歌单 | `k` / `↑` | 上移选择 |
| 添加到歌单 | `Enter` | 确认添加到选中歌单 |
| 添加到歌单 | `Esc` / `q` | 取消 |
| 帮助 | `?` / `Esc` / `q` | 关闭帮助 |

---

## 7. 测试策略

### 7.1 单元测试

| 模块 | 测试内容 | 优先级 |
|------|----------|--------|
| `bilibili/wbi` | WBI 签名算法正确性 | P0 |
| `bilibili/wbi` | mixin key 推导 | P0 |
| `bilibili/search` | 搜索结果解析 | P0 |
| `bilibili/video` | 视频信息解析 | P0 |
| `bilibili/stream` | DASH 响应解析 + 音频质量选择 | P0 |
| `queue` | 队列操作 (添加/移除/跳转) | P0 |
| `queue` | Shuffle 排列生成与切换 | P0 |
| `queue` | Repeat 模式切换 | P0 |
| `playlist` | 歌单 CRUD | P0 |
| `playlist/storage` | JSON 序列化/反序列化 | P0 |
| `config` | 配置加载/保存 | P1 |
| `command` | 命令解析 | P1 |

### 7.2 集成测试

| 场景 | 测试内容 | 优先级 |
|------|----------|--------|
| 搜索到播放 | 搜索关键词 → 选择结果 → 获取流 URL → 播放 | P0 |
| B 站 API 实际调用 | 用真实 SESSDATA 调用各端点，验证响应格式 | P1 |
| 歌单持久化 | 创建歌单 → 添加曲目 → 重启 → 验证恢复 | P0 |
| 队列恢复 | 添加队列 → 退出 → 重启 → 验证恢复 | P1 |

### 7.3 API Mock 测试

使用 `mockito` mock B 站 API 响应，测试:
- WBI 签名是否正确附加到请求
- 各种 API 响应格式解析
- 错误响应处理 (403, 412, -352 等)
- WBI key 过期后自动刷新

### 7.4 TUI 测试

- 可测试部分: 数据模型到 UI 状态的映射、命令处理逻辑
- 不做像素级渲染测试（维护成本过高）
- 手动测试清单: 见下方

### 7.5 手动测试清单

- [ ] 启动 TUI，确认布局正常
- [ ] 搜索关键词，确认结果列表
- [ ] 播放曲目，确认音频输出
- [ ] 暂停/继续，确认状态切换
- [ ] 下一首/上一首，确认切换正常
- [ ] 音量调节，确认变化
- [ ] Shuffle 切换，确认随机播放
- [ ] Repeat 切换，确认三种模式
- [ ] 创建歌单 (`c` 在歌单栏)，弹出输入 popup，输入名称后 Enter 确认
- [ ] 空名称时显示错误提示，Esc 取消无副作用
- [ ] `Shift+A` 弹出歌单选择 popup，选中后曲目加入歌单
- [ ] `d` 在歌单栏弹出确认 popup，Enter 删除，Esc 保留
- [ ] `d` 在歌单曲目视图（中栏）从歌单移除曲目
- [ ] Enter 选中歌单曲目，将歌单替换队列并播放
- [ ] 创建歌单，添加曲目，重启后确认保留
- [ ] 封面图显示（在支持图片的终端中）
- [ ] 窗口缩放，确认布局自适应
- [ ] 无 SESSDATA 启动，确认降级提示
- [ ] 无效 SESSDATA，确认错误提示
- [ ] 网络断开后恢复，确认行为
- [ ] `bili-player-cli play BV1xx...` 直接播放

---

## 8. 交付分期

### Phase 1: TUI 框架 + 核心播放 (保守版)

目标: 进入 TUI，能搜、能播、能控制。架构为后续阶段留扩展空间。

- TUI 三栏框架 (yazi/ranger 风格 + popup overlay 系统)
- App 主循环 (tokio::select! + event/command)
- 左栏: 仅"队列" (P2 扩展为歌单列表)
- 中栏: 曲目列表
- 右栏: 曲目详情 (文字)
- Header: app 名称 + 登录状态
- Status: 进度条 + 时间 + 音量数值
- 搜索 overlay (`/` 触发，居中 popup)
- 帮助 overlay (`?` 触发，全屏)
- 音量滑钮 overlay (调音量时弹出，自动消失)
- 窄终端自适应
- 播放控制 (暂停/继续/上下曲/seek/音量/静音)
- mpv 事件线程完整实现
- SESSDATA 配置 + 未登录提示
- 单元测试: Queue、Command

### Phase 2: 歌单与封面

目标: 可用性提升。

- 歌单 CRUD + 持久化（popup 交互：创建/删除/添加）✅
- 左栏扩展: 多歌单列表（Queue 虚拟条目 + 用户歌单，h/l 列焦点导航）✅
- 队列持久化 + Shuffle + Repeat ✅
- 封面图显示 (右栏 StatefulImage，占上 50% 高度) ✅
- BV 号/URL 直接播放 ✅
- 集成测试

### Phase 3: 完善

目标: 体验打磨。

- 音量归一化
- 快捷键自定义
- 搜索翻页/历史
- MPRIS (Linux)
- 主题切换
- 中文对齐
- 进度条交互跳转
